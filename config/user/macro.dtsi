/*
#define MACRO(NAME, TYPE, WAIT_TERM, TAP_TERM, ...)	\
    NAME: macro_##NAME { 									\
        label = ZMK_HELPER_STRINGIFY(macro_ ## NAME);		\
		ZMK_BEHAVIOR_CORE_ ## TYPE;							\
        wait-ms = <WAIT_TERM>; 								\
        tap-ms = <TAP_TERM>; 								\
    	__VA_ARGS__  		                            	\
};

"zmk,behavior-macro"
"zmk,behavior-macro-one-param"
"zmk,behavior-macro-two-param"
*/

#define MACRO_TAP_TERM 10
#define MACRO_WAIT_TERM 10

macros {
	
	/*
	2023-08-24 
	The below is temporary because Nick Conway started his dev branch before main zmk released parameterized macros

	#TODO remove this in the future
	*/

	ZMK_BEHAVIOR(mbrc, macro,
		wait-ms = <MACRO_WAIT_TERM>;
		tap-ms = <MACRO_TAP_TERM>;
		bindings = 
			<&kp LBRC>
			, <&kp RBRC>
			, <&kp LEFT>;
	)

	ZMK_BEHAVIOR(mlgt, macro,
		wait-ms = <MACRO_WAIT_TERM>;
		tap-ms = <MACRO_TAP_TERM>;
		bindings = 
			<&kp LGT>
			, <&kp GT>
			, <&kp LEFT>;
	)

	ZMK_BEHAVIOR(mbkt, macro,
		wait-ms = <MACRO_WAIT_TERM>;
		tap-ms = <MACRO_TAP_TERM>;
		bindings = 
			<&kp LBKT>
			, <&kp RBKT>
			, <&kp LEFT>;
	)

	ZMK_BEHAVIOR(mpar, macro,
		wait-ms = <MACRO_WAIT_TERM>;
		tap-ms = <MACRO_TAP_TERM>;
		bindings = 
			<&kp LPAR>
			, <&kp RPAR>
			, <&kp LEFT>;
	)

	ZMK_BEHAVIOR(mbslh, macro,
		wait-ms = <MACRO_WAIT_TERM>;
		tap-ms = <MACRO_TAP_TERM>;
		bindings = 
			<&kp BSLH>
			, <&kp BSLH>;
	)

	ZMK_BEHAVIOR(mfslh, macro,
		wait-ms = <MACRO_WAIT_TERM>;
		tap-ms = <MACRO_TAP_TERM>;
		bindings = 
			<&kp FSLH>
			, <&kp FSLH>;
	)

	ZMK_BEHAVIOR(mfslh, macro,
		wait-ms = <MACRO_WAIT_TERM>;
		tap-ms = <MACRO_TAP_TERM>;
		bindings = 
			<&kp FSLH>
			, <&kp FSLH>;
	)

	ZMK_BEHAVIOR(mne, macro,
		wait-ms = <MACRO_WAIT_TERM>;
		tap-ms = <MACRO_TAP_TERM>;
		bindings = 
			<&kp SPACE>
			, <&kp EXCL>
			, <&kp EQUAL>
			, <&kp SPACE>;
	)
};